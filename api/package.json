{
  "name": "server",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
   "generate": "npx prisma generate",
    "migrate": "npx prisma migrate dev --name init",
    "start": "node api/index.js",
    "build": "npx prisma generate && npx prisma migrate deploy && node api/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-jwt": "^8.5.1",
    "jwks-rsa": "^3.2.0"
  },
  "devDependencies": {
    "prisma": "^6.10.1"
  }
}
